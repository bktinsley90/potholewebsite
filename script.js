//  need to write a function that checks credntial 
function logIn(){
    let userName = prompt(`Enter Username`);
    let password = prompt(`Enterpassword`);
    function logMein(userName, password){
        if (userName === "admin"){
            
        }
    }


}
//  need to write another function that will input the name of a pothole and print on the page